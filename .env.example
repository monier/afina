# Afina Environment Configuration
# 
# Accepted Environments:
# - dev: default for local development
# - test: testing environment (unit tests + integration tests)
# - sandbox: staging environment
# - prod: production environment

# Ports
API_PORT=5100
WEB_PORT=3000
DB_PORT=5432
PGADMIN_PORT=5050

# Database
DB_HOST=postgres
DB_NAME=afina_db
DB_USER=postgres
DB_PASSWORD=postgres

# API
ENV=dev
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# Web
FRONTEND_API_BASE_URL=http://localhost:5100/api/v1

# pgAdmin
PGADMIN_EMAIL=admin@example.com
PGADMIN_PASSWORD=admin

# Docker Compose Profiles
# Available profiles: db-view (includes pgAdmin for database administration)
COMPOSE_PROFILES=db-view
